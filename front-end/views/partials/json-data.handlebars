fluid.registerNamespace("ul.config");
ul.config.server = {{#if config}}{{#jsonify config}}{{/jsonify}}{{else}}{}{{/if}};

fluid.defaults("ul.components.data", {
    gradeNames: ["fluid.modelRelayComponent", "sourceAware", "autoInit"]
});
var data = ul.components.data();